--- /dev/null
+++ b/setup.py
@@ -0,0 +1,3 @@
+from setuptools import setup
+
+setup()
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -76,3 +76,10 @@ asyncio_mode = "strict"
 requires = ["poetry-core>=1.0.0"]
 build-backend = "poetry.core.masonry.api"
 
+[project]
+name = "reactivex"
+dynamic = ["version"]
+
+[tool.setuptools.packages.find]
+where = ["."]
+include = ["reactive*"]
